# Example environment variables for civic-scout-agent
# Copy this file to `.env` locally and fill in real values.
# Do NOT commit your real `.env` — use Netlify environment variables for deployment.

# Google Gemini API Key (required for AI analysis)
GEMINI_API_KEY=your_gemini_api_key_here

# NYC Legistar API Token (required for fetching events)
LEGISTAR_API_TOKEN=your_legistar_api_token_here

# Google Maps API Key (required for geocoding)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# NYC Open Data App Token (optional, for better rate limits)
NYC_OPEN_DATA_TOKEN=your_nyc_open_data_token_here

# NOTE: If these values were previously committed, remove them from git history
# using `git filter-repo` or the BFG Repo-Cleaner before pushing. See README.
